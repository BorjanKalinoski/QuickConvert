{
  "name": "quickconvert",
  "scripts": {
    "heroku-postbuild": "cd express && npm install && cd ../react && npm install && npm run build",
    "start": "cd express && npm start",
    "postinstall": "cd express && npm run tsc"
  },
  "engines": {
    "node": "12.x",
    "npm": "6.x"
  }
}